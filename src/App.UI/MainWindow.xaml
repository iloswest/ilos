<Window x:Class="AppUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обувной магазин" Height="550" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2E8B57" Padding="10">
            <TextBlock Text="Обувной магазин" FontSize="24" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBox x:Name="LoginBox" Width="150" Margin="5" Text="admin"/>
            <PasswordBox x:Name="PasswordBox" Width="150" Margin="5"/>
            <Button x:Name="LoginButton" Content="Войти" Width="80" Margin="5" Click="LoginButton_Click"/>
            <Button x:Name="GuestButton" Content="Гость" Width="80" Margin="5" Click="GuestButton_Click"/>
            <TextBlock x:Name="UserInfo" Margin="20,0,0,0" FontWeight="Bold"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <ListBox x:Name="ProductsList">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10" CornerRadius="8">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="80" Height="80" Background="LightGray" CornerRadius="5">
                                    <TextBlock Text="📷" FontSize="30" TextAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Margin="10,0,0,0" Width="250">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{Binding Brand}" Foreground="Gray"/>
                                    <TextBlock Text="{Binding CategoryName}" Foreground="Gray" FontSize="10"/>
                                </StackPanel>

                                <StackPanel Margin="10,0,0,0" Width="120">
                                    <TextBlock Text="{Binding Provider}" FontSize="11"/>
                                    <TextBlock Text="В наличии: " FontSize="11"/>
                                    <TextBlock Text="{Binding Count}" FontWeight="Bold"/>
                                </StackPanel>

                                <StackPanel Margin="10,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Price, StringFormat={}{0:C}}" FontSize="14"/>
                                        <TextBlock Text=" → " FontSize="14" Margin="5,0"/>
                                        <TextBlock Text="{Binding FinalPrice, StringFormat={}{0:C}}" FontSize="16" FontWeight="Bold" Foreground="Green"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Window>